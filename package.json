{
  "name": "@modelcontextprotocol/server-discord-webhook",
  "version": "0.1.0",
  "description": "MCP server for sending messages via Discord webhooks",
  "type": "module",
  "license": "MIT",
  "author": "Your Name",
  "homepage": "https://github.com/shehdrbs123/mcp-server-discord-webhook",
  "bin": {
    "mcp-server-discord-webhook": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.0.1"
  },
  "devDependencies": {
    "@types/node": "^22",
    "shx": "^0.3.4",
    "typescript": "^5.6.2"
  }
}
